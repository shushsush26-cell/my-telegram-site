{
  "name": "my-telegram-site",
  "version": "1.0.0",
  "description": "Site for sending messages to Telegram",
  "scripts": {
    "build": "echo 'No build step needed'",
    "vercel-build": "echo 'No build step needed'"
  },
  "static": true
}
{
  "dependencies": {
    "node-fetch": "^2.6.7"
  }
}
